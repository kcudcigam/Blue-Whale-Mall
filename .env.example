# 服务器配置
PORT=3001
NODE_ENV=development

# 前端地址（用于 CORS）
FRONTEND_URL=http://localhost:5173

# JWT 密钥（生产环境请使用复杂的随机字符串）
JWT_SECRET=bluewhalemall-jwt-secret-key-change-in-production

# 数据加密密钥（必须是32字符）
ENCRYPTION_KEY=bluewhalemall-secret-key-32-chars!!

# 数据库路径（可选，默认在 server/database 目录）
# DB_PATH=./server/database/bluewhalemall.db
